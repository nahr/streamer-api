; Stunnel config for Facebook Live RTMPS relay
; Run: stunnel stunnel-fb.conf
; Then set USE_STUNNEL_FOR_RTMPS=1 when starting the API

[fb-live]
client = yes
accept = 127.0.0.1:19350
connect = live-api-s.facebook.com:443
verifyChain = no
